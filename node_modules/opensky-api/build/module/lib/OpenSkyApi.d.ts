import { Credentials } from '../types/Credentials';
import { Flight } from '../types/Flight';
import { StateVector } from '../types/StateVector';
import { BoundingBox } from './BoundingBox';
export declare class OpenSkyApi {
    private static HOST;
    private static API_ROOT;
    private static STATES_URI;
    private static MY_STATES_URI;
    private static FLIGHTS_URI;
    private static FLIGHTS_BY_AIRCRAFT_URI;
    private static FLIGHTS_BY_ARRIVAL_URI;
    private static FLIGHTS_BY_DEPARTURE_URI;
    private _axios;
    private authenticated;
    private lastRequestTime;
    constructor(credentials?: Credentials);
    getFlights(beginTime: number, endTime: number): Promise<Flight[]>;
    getFlightsByArrivalAirport(airport: string, beginTime: number, endTime: number): Promise<Flight[]>;
    getFlightsByDepartureAirport(airport: string, beginTime: number, endTime: number): Promise<Flight[]>;
    getFlightsByAircraft(icao24: string, beginTime: number, endTime: number): Promise<Flight[]>;
    getStates(time: number | null, icao24: string[] | null, bbox?: BoundingBox | null): Promise<{
        time: number;
        states: StateVector[];
    }> | null;
    getMyStates(time: number | null, icao24: string[] | null, serials: number[]): Promise<{
        time: number;
        states: StateVector[];
    }> | null;
    private getOpenSkyStates;
    private getOpenSkyFlights;
    private checkRateLimit;
}
export { BoundingBox } from './BoundingBox';
